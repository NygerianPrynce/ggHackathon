/**
 * Copy this file to `openai-secrets.js` in the same folder and set your keys.
 * `openai-secrets.js` is gitignored — do not commit real API keys.
 *
 * After saving, reload the extension at chrome://extensions
 */
self.OPENAI_API_KEY = "";
self.ELEVENLABS_API_KEY = "";
self.ELEVENLABS_VOICE_ID = "";
